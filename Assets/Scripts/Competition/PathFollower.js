#pragma strict
/*
public var waypoints : List.<Transform> = List.<Transform>();
private var waypoint : Transform;

function FollowPath(){
	if (waypoints[waypoint].position != transform.position){
		transform.position = Vector3.MoveTowards(transform.position, waypoint.position, .5);
	}
	else{
		waypoint += 1;	
		if (waypoint > waypoints.Count){waypoint = 0;}
	}
}
*/